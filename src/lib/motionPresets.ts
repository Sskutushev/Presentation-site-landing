/**
 * Библиотека предустановленных анимаций
 *
 * Основная функция: предоставить переиспользуемые анимационные пресеты для компонентов
 * Используется в секциях лендинга для создания плавных появляющихся анимаций
 */

// Пресет: появление снизу
export const fadeInUp = {
  initial: { opacity: 0, y: 40 }, // Начальное состояние: прозрачный и смещен на 40px вниз
  whileInView: { opacity: 1, y: 0 }, // Видимое состояние: непрозрачный и на месте
};

// Пресет: анимация контейнера с задержкой для дочерних элементов
export const staggerContainer = {
  hidden: {}, // Скрытое состояние
  show: {
    transition: { staggerChildren: 0.15 }, // Показать с задержкой 0.15с между дочерними элементами
  },
};

// Пресет: появление слева
export const fadeInLeft = {
  initial: { opacity: 0, x: -60 }, // Начальное состояние: прозрачный и смещен на 60px влево
  whileInView: { opacity: 1, x: 0 }, // Видимое состояние: непрозрачный и на месте
};

// Пресет: появление справа
export const fadeInRight = {
  initial: { opacity: 0, x: 60 }, // Начальное состояние: прозрачный и смещен на 60px вправо
  whileInView: { opacity: 1, x: 0 }, // Видимое состояние: непрозрачный и на месте
};
